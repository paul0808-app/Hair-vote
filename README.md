# ヘアスタイル投票アプリ（Hair-vote）

美容室（PAUL西葛西店・COコ西葛西南口店）の各席のタブレットで、
お客様が好きなヘアスタイル写真に「いいね」して投票するアプリです。

---

## 開発の進みぐあい

| フェーズ | 内容 | 状態 |
|---|---|---|
| 1 | 土台づくり＋投票画面の見た目（仮の写真30枚） | ✅ 完了 |
| 2 | 写真の拡大モーダル・投票確認・完了画面・リセット | ✅ 完了 |
| 3 | Supabase 接続（写真をDBから取得／投票をDBに保存） | ✅ 完了 |
| 4 | 管理画面（ランキング・期間/店舗フィルター・CSV・写真管理） | ✅ 完了 |
| 5 | PWA 対応（ホーム画面に追加）と仕上げ | ✅ 完了 |

---

## いまできること

### 投票画面（トップ）
- ヘアスタイル写真を格子状（グリッド）に表示
  - iPad 横向き（1024px〜）＝ **5列** / iPad 縦向き（640〜1023px）＝ **4列** / スマホ＝ **3列**
- 写真を**1回タップ**すると**拡大モーダル**が開く（スタイル名・説明文・ハッシュタグを表示）
- 写真を**2回タップ**すると、拡大せずにその場で選択・解除できる
- **担当スタイリスト名はお客様には表示しない**。データとしては保存されており、
  管理画面のランキングとCSVでのみ確認できる
- 選択された写真は、**太いピンクの枠線・右上の番号バッジ・薄い暗色・チェックマーク**の4つが同時に付く
- 画面下の固定バーに `2枚 / 5枚 選択中` とドット（●●○○○）。0枚のときは「投票へ進む」が押せない
- 5枚そろうとバーの色が変わって「選択完了！」になる

### 写真の拡大モーダル
- 拡大した写真＋スタイル名・説明文・ハッシュタグ（担当スタイリスト名は出さない）
- 大きな**「いいね」ボタン**（選択済みなら「選択を解除」に変わる）
- **左右スワイプ**、または左右の矢印ボタンで前後の写真へ移動
- **×ボタン・背景タップ・下スワイプ**のいずれでも閉じられる
- 5枚選んだ状態で6枚目を押すと「最大5枚までです」というお知らせが2秒だけ出る

### 投票確認ダイアログ
- 「本当に投票しますか？」＋選んだ写真のサムネイル
- 5枚のときは「選んだ5枚で投票を確定します。確定後は変更できません。」
- 5枚未満でも投票でき、その場合は「3枚で投票します。よろしいですか？」のように枚数が入る

### 投票完了画面
- 「ご投票ありがとうございました」＋選んだ写真
- 画面下に「次のお客様へ」ボタン。**1タップ**でリセットし、次のお客様の投票を受け付ける

### 投票セッションの決まりごと
- 初回アクセス時に `crypto.randomUUID()` でセッションIDを作り、ブラウザに保存
- URLに `?seat=A1` のように席番号を付けると、そのセッションに記録される
- 選択途中の状態はブラウザに保存され、閉じても開き直せば復元される
- 投票を確定するとその回は**読み取り専用**になり、変更や追加投票はできない
- 確定から**30分**たつ、または「次のお客様へ」を押すと、自動的に次の投票を受け付ける

### データベース（Supabase）への保存
- 写真の一覧を `styles` テーブルから取得（5分間キャッシュしてDBへの負荷を抑える）
- 「投票を確定する」を押した**その1回だけ**データベースに書き込む
- 保存はデータベース側の関数 `submit_ballot` が**1つのトランザクション**として実行するので、
  途中で失敗しても中途半端なデータが残らない
- 枚数の上限（5枚）・重複・存在しない写真のチェックを**サーバー側でも**行う
- URLに `?salon=店舗コード` を付けると、その店舗として記録される

### 管理画面（`/admin?key=合言葉`）
- **コンテストの集計（3つの表彰）**
  1. **スタイル別**：いいねが多い順（順位・写真・スタイル名・担当・得票数・得票率）
  2. **スタイリスト別**：合計いいね数が多い順（出品数もあわせて表示）
  3. **店舗賞**：合計いいね数 ÷ 所属スタッフ数 ＝ 1人あたりいいね数が多い順
  （同数なら同順位。3つともCSVに書き出されます）
- **スタイリスト名簿**：写真の担当者はこの名簿から選ぶ方式。手入力による
  表記ゆれ（「田中 美咲」と「田中美咲」が別人になる）を防ぐ。
  **店舗賞の分母は、この名簿に登録した在籍スタッフの人数**
  - 名前を1行に1人ずつ貼り付けて**まとめて登録**できる
  - 行の先頭に店舗名を書けば（例：`COコ　松本　尚弥`）、全店まとめて1回で登録できる
  - すでにいる人は自動で飛ばすので、同じものを貼り直しても重複しない
- **期間の絞り込み**：今日 / 直近7日 / 今月 / 全期間 / 日付を指定（すべて日本時間で計算）
- **店舗の絞り込み**：全店 / PAUL / COコ / 行徳 / Ali&LEVEL
- **サマリー**：総投票数（人数）・総いいね数・平均選択枚数
- **CSVダウンロード**：BOM付きUTF-8なので、Excelで開いても文字化けしない
- **集計のリセット**：ボタンひとつで総投票数・総いいね数・ランキングを0に戻せる。
  **最終リセット日時**を画面とCSVに表示する。
  投票データは消さず「ここから先を数える」開始点を記録するだけなので、取り消して元に戻せる
- **写真の管理**：写真の追加・キャプション等の編集・並び順の変更・非表示（アーカイブ）
- **サンプル写真の一括非表示**：最初から入っているサンプル30枚を、ボタンひとつでまとめて隠せる
- **並び順のランダム化**：ボタンひとつで全体の並びを混ぜ直せる（押すたびに変わる）。
  いつも同じ写真が上にあると票が偏るため、ときどき混ぜ直すのに使う
- **店舗URL**：各店舗・各席のURLの一覧とコピーボタン、ホーム画面への追加手順
- 写真はiPadから直接アップロードでき、**送る前にブラウザ内で自動的に縮小**される
  （数MBの写真が100KB前後になり、投票画面の表示が速くなる）

**写真について**
初期状態ではサンプル画像（picsum.photos）が入っています。
管理画面から実際のヘアスタイル写真を登録すると、Supabase Storage（東京リージョン）から
配信されるようになり、表示が速くなります。
通信が遅いときは、3.5秒でいったんスタイル名だけのタイルに切り替わりますが、
**読み込みはそのまま続いていて、届いた写真から順に写真表示に切り替わります**。
読み込みに失敗した場合も、間隔を空けて最大4回まで自動で取り直します。

### 参加店舗とタブレットのURL

各店舗のタブレットは、次のURLを開いておきます（席番号は任意）。

| 店舗 | 店舗コード | 席数 | タブレットで開くURL（1席目の例） |
|---|---|---|---|
| PAUL | `PAUL` | 8席 | `https://〇〇.vercel.app/?salon=PAUL&seat=1` |
| COコ | `COCO` | 6席 | `https://〇〇.vercel.app/?salon=COCO&seat=1` |
| 行徳 | `GYOTOKU` | 4席 | `https://〇〇.vercel.app/?salon=GYOTOKU&seat=1` |
| Ali&LEVEL | `ALI_LEVEL` | 7席 | `https://〇〇.vercel.app/?salon=ALI_LEVEL&seat=1` |

全25席ぶんのURLは、管理画面の「店舗URL」タブに一覧で並びます。
席が増減したときは `lib/salons.ts` の `SALON_SEAT_COUNTS` の数字を変えてください。

`?salon=` を付けずに開いても投票はできます（店舗が未記録になるだけです）。

> 店舗を増やしたい・表示名や席数を変えたいときは `lib/salons.ts` の1か所を直すだけで、
> 投票URL・管理画面の絞り込み・写真の登録フォーム・CSV のすべてに反映されます。

### タブレットをアプリのように使う（PWA）
- 「ホーム画面に追加」すると、**アドレスバーが消えて全画面**で開く
  （お客様が誤って他のページへ移動することを防げる）
- ホーム画面のアイコンはピンク地に白いハート
- 一度 `?salon=...&seat=...` 付きで開けば、**そのタブレットが店舗と席番号を覚える**ので、
  次からアイコンをタップするだけで同じ設定で使える
- 画面を引き下げての再読み込みや、写真の長押しメニューが出ないようにしてある

### データベースの自動停止対策
- Supabase の無料プランは1週間ほどアクセスが無いと一時停止する
- 1日1回、アプリが自動でデータベースに軽く読み取りを行い、停止を防ぐ
  （`/api/keep-alive` を Vercel の定期実行から呼び出す。追加料金なし）

**接続していないときの動作**
環境変数が未設定、またはデータベースにつながらないときは、
画面が真っ白になったりせず**仮データで表示**され、写真の枚数の横に「（仮データ）」と出ます。
その状態では投票は保存されません（保存に失敗した場合は完了画面に進まず、お知らせが出ます）。

---

## Supabase の設定手順（iPadだけで完了します・所要15分ほど）

**用語**：Supabase＝データを保存しておく置き場（データベース）のサービスです。

### 1. プロジェクトを作る

1. Safari で **https://supabase.com** を開き、ログイン
2. **Dashboard**（最初に出る画面）で **「New project」** を押す
3. 入力するもの
   - **Name**：`hair-vote`（何でもOK）
   - **Database Password**：自動生成のままでOK。**このパスワードは今回使いません**
   - **Region**：**Northeast Asia (Tokyo)** を選ぶ（日本から一番速い）
4. **「Create new project」** を押して、1〜2分待つ

### 2. テーブルを作る

1. 左側のメニューから **「SQL Editor」**（紙とペンのようなアイコン）を開く
2. **「New query」** を押す
3. このリポジトリの **`supabase/schema.sql`** の中身を**すべて**コピーして貼り付ける
   - GitHubアプリやSafariで `supabase/schema.sql` を開き、全選択してコピー
4. 右下の **「Run」** を押す。`Success. No rows returned` と出れば成功
5. 同じ手順で **`supabase/seed.sql`**（サンプル写真30件）も貼り付けて **Run**
6. 左メニューの **「Table Editor」** を開き、`styles` に30行入っていれば完了

### 3. 接続情報を2つコピーする

左下の **「Project Settings」**（歯車アイコン）→ **「API」** を開きます。
（新しい画面では **「API Keys」** という項目名になっている場合があります）

コピーするのは次の2つです。

| 何をコピーするか | どこにあるか | Vercelでの名前 |
|---|---|---|
| プロジェクトのURL | **Project URL** の欄（`https://〇〇.supabase.co`） | `SUPABASE_URL` |
| 秘密の鍵 | **Project API keys** の **`service_role`** の行の「Reveal」を押して表示される長い文字列 | `SUPABASE_SERVICE_ROLE_KEY` |

> 新しいSupabaseの画面では、`service_role` は **「Legacy API keys」** タブの中にあります。
> または **「Secret keys」** にある `sb_secret_...` で始まる鍵でも構いません。
>
> ⚠️ **`service_role` の鍵は絶対に人に見せないでください。** これ1つでデータベースを
> 全部操作できます。`anon` / `publishable` のほうの鍵ではないので注意してください。

### 4. Vercel に貼り付ける

1. **https://vercel.com** → プロジェクト **Hair-vote** を開く
2. 上部の **「Settings」** タブ → 左メニューの **「Environment Variables」**
3. 次の2つを追加する（**Key** に名前、**Value** に貼り付け、**Add** を押す）

   | Key | Value |
   |---|---|
   | `SUPABASE_URL` | 手順3でコピーした Project URL |
   | `SUPABASE_SERVICE_ROLE_KEY` | 手順3でコピーした service_role の鍵 |

   環境（Production / Preview / Development）は**すべてにチェック**を入れてください。
4. 上部の **「Deployments」** タブ → いちばん上のデプロイの右の **「…」** → **「Redeploy」**
   - 環境変数は**貼っただけでは反映されません。必ず Redeploy が必要**です

### 5. 管理画面の合言葉を決める

管理画面は `https://〇〇.vercel.app/admin?key=合言葉` で開きます。
この合言葉を Vercel に登録してください。

1. Vercel → プロジェクト → **Settings** → **Environment Variables** → **Add Environment Variable**
2. **Key** に `ADMIN_KEY`
3. **Value** に、自分で決めた推測されにくい長めの文字列
   （例：`paul-coco-2026-7fk29xq`。お客様には見せず、スタッフ間だけで共有します）
4. 環境は Production / Preview / Development すべてにチェックして **Save**
5. **Deployments** → 最新の「…」→ **Redeploy**

合言葉が違う場合は404になり、管理画面があること自体が分かりません。

### 6. つながったか確認する

投票URLを開いて、写真の枚数の横に **「（仮データ）」が出ていなければ接続成功**です。
出ている場合は、環境変数の名前のスペルと、Redeploy をしたかを確認してください。

---

## iPad だけでの公開手順（初回だけ・所要10分ほど）

PCは不要です。iPad の Safari だけで完了します。

### 1. Vercel にログインする

1. Safari で **https://vercel.com** を開く
2. 右上の **「Sign Up」**（または Log In）→ **「Continue with GitHub」** を選ぶ
3. GitHub のアカウントでログインし、Vercel からのアクセスを許可する

### 2. このリポジトリを取り込む

1. ログイン後の **Dashboard**（最初に出る一覧画面）で **「Add New...」→「Project」** を押す
2. **「Import Git Repository」** の一覧から **`Hair-vote`** を探して **「Import」** を押す
   - 一覧に出ないときは、その下の **「Adjust GitHub App Permissions」** から `Hair-vote` を許可する
3. **Framework Preset** が **Next.js** になっていることを確認（自動で入ります）
4. 設定は何も変えずに **「Deploy」** を押す

### 3. できあがったURLを開く

- 1〜2分待つと「Congratulations」の画面になります
- 表示された **`https://hair-vote-〇〇〇.vercel.app`** のURLが投票画面です
- このURLをタブレットの Safari で開いてください

### 4. 開発中のブランチを見るには

いまの作業は `main` ではなく **`claude/hairstyle-voting-app-5w5suj`** というブランチにあります。

- Vercel の Dashboard → プロジェクト名 → 上部の **「Deployments」** タブを開く
- ブランチ名が `claude/hairstyle-voting-app-5w5suj` の行をタップ
- **「Visit」** ボタンでプレビューURLが開きます（このURLで今の画面を確認できます）

> 以降は、このリポジトリに変更を push するたびに Vercel が自動でURLを更新します。
> 何も操作は要りません。

---

## 動作確認チェックリスト（iPadで操作してください）

### 写真の表示を速くする

**いちばん効果があるのはサンプル写真を片付けることです。**
サンプル30枚は外部の無料サービス（picsum.photos）から配信されていて遅く、
投票画面全体の読み込みを重くします。

- [ ] 実際のヘアスタイル写真をいくつか登録する
- [ ] 管理画面 → **「写真の管理」** タブのいちばん上に出る
      **「サンプル写真 ○枚 をまとめて非表示にする」** を押す
- [ ] 投票画面を開き直すと、登録した写真だけが**すぐに**表示される

**読み込みの挙動**
- [ ] 通信が遅いときは、いったんスタイル名のタイルになる
- [ ] そのまま待っていると、**届いた写真から順に写真表示に変わる**
- [ ] 写真が1枚も登録されていないと「ただいま準備中です」と出る（サンプルは出ない）

### 集計のリセット（管理画面）

事前に、Supabase の SQL Editor で下記を1回実行してください。

```sql
create table if not exists app_settings (
  key        text primary key,
  value      text,
  updated_at timestamptz not null default now()
);
alter table app_settings enable row level security;
```

- [ ] ランキングタブに **「集計のリセット」** の欄がある
- [ ] 「集計をリセット」→「本当にリセットする」で総投票数などが **0** になる
- [ ] **最終リセット：2026年○月○日 ○○:○○** と表示される
- [ ] 「リセットを取り消して、これまでの集計に戻す」で元の数字に戻る

### コンテストの集計（管理画面）

事前に、Supabase の SQL Editor で `supabase/schema.sql` を**もう一度まるごと実行**してください。
スタイリスト名簿と集計の機能が追加されます（何度実行しても壊れません）。
すでに手入力していた担当者名は、自動で名簿に取り込まれます。

- [ ] 「スタイリスト」タブの **「名簿をまとめて登録する」** に、
      `店舗名　氏名` の形で1行に1人ずつ貼り付け →「まとめて名簿に追加する」
- [ ] タブ上部の在籍人数が、実際の在籍人数と合っている
- [ ] サンプルとして自動で入った担当者は、
      **「サンプルのスタイリスト ○人 を削除する」** でまとめて消せる
      （担当写真がサンプルだけの人だけが対象。実際のスタッフは消えない）
- [ ] 各スタイリストの**所属店舗を設定する**（設定しないと店舗賞に入りません）
- [ ] 出品しないスタッフも含め、**在籍している全員を登録する**
      （ここに登録した人数が、店舗賞の「÷ 所属スタッフ数」の分母になります）
- [ ] タブ上部に「PAUL ○人 / COコ ○人 …」と在籍人数が出る
- [ ] 「ランキング」タブに3つの表彰が並ぶ
      （①スタイル別 ②スタイリスト別 ③店舗賞）
- [ ] CSVをダウンロードすると、3つの表彰すべてが入っている
- [ ] 「写真の管理」で写真を追加するとき、担当スタイリストが**プルダウン**になっている

### 並び順のランダム化（管理画面）
- [ ] 「写真の管理」タブの「登録済みのスタイル」の右に
      **「並び順をランダムにする」** ボタンがある
- [ ] 押すと並びが変わり、「○枚の並び順をランダムにしました」と出る
- [ ] もう一度押すと、また違う並びになる
- [ ] 投票画面を開き直すと、その並びで表示される

### 2回タップで選択（投票画面）
- [ ] 写真を**1回タップ**すると拡大モーダルが開く
- [ ] 写真を**2回続けてタップ**すると、拡大せずに選択される
- [ ] もう一度2回タップすると解除される

### タブレットの設定（店舗ごとに1回だけ）
- [ ] 管理画面 → **「店舗URL」** タブに、PAUL 8席・COコ 6席・Ali&LEVEL 7席・行徳 4席の
      合計25席ぶんのURLが並んでいる
- [ ] 使う席のURLの「コピー」（または「○席ぶんまとめてコピー」）を押す
- [ ] タブレットのSafariでそのURLを開く
- [ ] 共有ボタン（□に↑）→ **「ホーム画面に追加」** → 「追加」
- [ ] アイコンから開くと、アドレスバーが出ない全画面になる

---

## 技術メモ（開発者向け）

- Next.js 16（App Router）+ TypeScript + Tailwind CSS v4
- 選択状態は React の state と `localStorage` のみで管理し、**データベースには書き込まない**
  （投票確定時に1回だけ書き込む設計。フェーズ3で実装）

### 同時接続について

タブレット1台＝1ブラウザ＝独立した `localStorage` なので、何台から同時に開いても
互いの選択状態に影響しない。5台同時に投票・確定・リセットする自動テストで、
セッションIDと選択内容がそれぞれ独立していることを確認済み。

フェーズ3でデータベースに接続したあとも、書き込みは「お客様1人あたり確定時の1回だけ」に
限定する。あわせて `UNIQUE (ballot_id, style_id)` 制約と、カウンターを持たず `COUNT` で
集計する方針により、同時書き込みで票数がずれないようにする。

### 管理画面の安全対策

- URLの `?key=` を環境変数 `ADMIN_KEY` と照合し、違えば404を返す
- 画面の表示時だけでなく、**保存や削除などの操作のたびに毎回**合言葉を確認する
  （これらの処理はインターネットから直接呼び出せるため）
- `/admin` には `Referrer-Policy: no-referrer` と `X-Robots-Tag: noindex` を付け、
  合言葉が他サイトに渡ったり検索に載ったりしないようにしている

```
app/
  layout.tsx              全ページ共通の枠組み・アプリの設定
  page.tsx                投票画面（トップ）
  actions.ts              投票をデータベースに保存する処理
  manifest.ts             ホーム画面に追加したときの設定（PWA）
  icon.png / apple-icon.png  アプリのアイコン
  admin/page.tsx          管理画面
  admin/actions.ts        写真の登録・編集・並び替え・表示切替
  admin/csv/route.ts      ランキングのCSV出力（BOM付きUTF-8）
  api/keep-alive/route.ts データベースの自動停止を防ぐ定期アクセス
  globals.css             共通スタイル・配色
components/
  vote-screen.tsx         投票画面の本体
  style-card.tsx          グリッドの写真1枚
  photo-modal.tsx         写真の拡大モーダル
  confirm-dialog.tsx      投票確認ダイアログ
  thanks-screen.tsx       投票完了画面＋リセット
  bottom-bar.tsx          画面下の固定バー
  thumb.tsx               小さなサムネイル
  toast.tsx               短いお知らせ
  admin/filters.tsx       期間・店舗の絞り込み
  admin/summary-cards.tsx 総投票数などのカード
  admin/ranking-table.tsx ランキング表
  admin/style-manager.tsx 写真の管理
  admin/salon-urls.tsx    各店舗のURL一覧
  admin/reset-panel.tsx   集計のリセットと最終リセット日時
  admin/style-form-fields.tsx 追加・編集の共通入力欄
lib/
  types.ts                データの形の定義
  salons.ts               参加店舗の定義（ここ1か所で店舗を増やせる）
  supabase.ts             Supabase への接続（サーバー側専用）
  styles.ts               写真一覧の取得
  admin.ts                管理画面のデータ取得（サーバー側専用）
  admin-shared.ts         管理画面の型・ラベル・期間の計算
  admin-auth.ts           管理画面の合言葉の確認
  image-resize.ts         アップロード前の写真の縮小（ブラウザ側）
  dummy-styles.ts         仮の写真データ30件
  use-vote-session.ts     投票セッションと端末の設定の管理
public/
  icon-192.png / icon-512.png  ホーム画面用のアイコン
supabase/
  schema.sql              テーブル・制約・関数・写真の保存場所
  seed.sql                サンプル写真30件
vercel.json               フレームワーク指定と定期実行の設定
```
